V2 Phase 9
1. Add step-by-step chain-of-thought reasoning and quality evaluation logic inside the agent.
Craft a detailed Gemini prompt template that forces the agent to evaluate each matched insight for relevance to NSE or user query, check reputation score against a 50-point threshold, calculate value-for-price, and output a clear “buy” or “skip” decision with visible reasoning steps; integrate this into the LangChain chain so every purchase decision is transparent, auditable, and aligned with real expert data quality standards.










To-Do:
1. Open the main agent file.
Go to backend/agent.py (created in Point 7) and scroll to the section where the SYSTEM_PROMPT is defined — this is where you will extend the agent’s reasoning logic.
2. Create a dedicated CoT evaluation prompt template.
Right below the existing SYSTEM_PROMPT, add a new multi-line string called EVALUATION_PROMPT_TEMPLATE that instructs the agent to always think step-by-step: 1) Relevance to user query and NSE markets, 2) Reputation score check (must be ≥ 50), 3) Value-for-price calculation, 4) Final clear decision.
3. Craft the detailed evaluation instructions inside the template.
Write the prompt to force this exact sequence: “First, rate relevance 0-100 to the user query and NSE context. Second, check on-chain reputation — if <50, immediately SKIP. Third, calculate value-for-price (relevance / price). Fourth, output ONLY in this format: Reasoning: [visible step-by-step text] Decision: BUY or SKIP” so every response is transparent and auditable.
4. Update the LangChain prompt to include the new CoT template.
Modify the ChatPromptTemplate to insert the EVALUATION_PROMPT_TEMPLATE after the semantic search tool returns results, so the agent is forced to run the full reasoning chain before any payment decision.
5. Add the evaluation step as a custom LangChain runnable.
Create a new async function async def evaluate_insights(state: dict) -> dict: that takes the semantic search results, injects them into the EVALUATION_PROMPT_TEMPLATE, and runs it through the Gemini model to get structured reasoning + decision.
6. Integrate the evaluation runnable into the agent chain.
Update the create_tool_calling_agent or AgentExecutor to include the new evaluate_insights step in the sequence (after semantic_search tool, before payment tool) so the agent must complete full CoT reasoning on every insight match.
7. Enforce the reputation threshold and value-for-price logic.
Inside the EVALUATION_PROMPT_TEMPLATE, explicitly hard-code the rules: “Reputation must be 50 or higher — otherwise SKIP” and “Value-for-price = relevance_score / price_in_usdc; only BUY if > 8.0” so the agent follows objective, consistent quality standards.
8. Make reasoning fully visible and auditable in the output.
Configure the prompt to require the agent to return its reasoning in a clear markdown block before the final Decision, and set verbose=True plus return_intermediate_steps=True in the AgentExecutor so every run logs the complete thought process.
9. Add structured output parsing for BUY/SKIP decisions.
Use LangChain’s PydanticOutputParser or a simple regex fallback to extract the exact “Decision: BUY” or “Decision: SKIP” string so the next tool (x402 payment) can reliably check the decision before triggering any payment.
10. Test the full reasoning flow locally with sample data.
At the bottom of agent.py, add a test block that runs asyncio.run(run_agent("Show me the best NIFTY 24500 call insight")) using the new CoT prompt; verify in the console that you see visible step-by-step reasoning, reputation check, value-for-price calculation, and a clear BUY or SKIP decision before moving to Point 10.