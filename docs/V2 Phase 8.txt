V2 Phase 8
1. Implement semantic search across on-chain listings using Gemini embeddings.
Build a custom LangChain tool that queries the deployed smart contract for all active ASAs, pulls metadata (price, reputation, IPFS hash), converts insight descriptions into embeddings via Gemini, and returns the top three matches ranked by relevance and reputation score; add caching and rate-limit handling so the agent can instantly surface high-quality human insights whenever a user asks for trading alpha.








To-Do:
1. Create a dedicated tools module for the semantic search tool.
Inside backend/ create a new folder tools (mkdir backend/tools) and add a file backend/tools/semantic_search.py — this keeps the tool clean and importable into agent.py later.
2. Install any missing embedding and utility packages.
In your activated venv run pip install langchain-google-genai numpy tenacity (this adds Gemini embeddings support and simple rate-limit retry logic without changing earlier dependencies).
3. Import everything needed in semantic_search.py.
At the top add: from langchain_google_genai import GoogleGenerativeAIEmbeddings, from langchain_core.tools import tool, from utils.ipfs import upload_insight_to_ipfs (we will extend it next), the InsightListingClient and ReputationClient from your contracts, from algosdk.v2client import indexer, import numpy as np, from tenacity import retry, stop_after_attempt, wait_fixed, from functools import lru_cache, and load_dotenv + os.
4. Extend the IPFS helper with a fetch function.
Open backend/utils/ipfs.py and add an async def fetch_insight_from_ipfs(cid: str) -> str that uses the Pinata gateway (or public ipfs.io) with your JWT to retrieve and return the plain text content of the insight.
5. Initialize the Gemini embeddings model once.
Create embeddings = GoogleGenerativeAIEmbeddings(model="embedding-004", google_api_key=os.getenv("GEMINI_API_KEY")) at module level so every tool call reuses the same efficient embedding instance.
6. Build the core semantic_search tool with the @tool decorator.
Add @tool above async def semantic_search(query: str) -> str: — inside it, first call the InsightListingClient to fetch all active listings (loop through a reasonable range or use indexer to find app calls), pull metadata (price, seller, ipfs_hash, reputation via ReputationClient), then fetch the full text from IPFS for each.
7. Generate embeddings and compute ranked results.
Convert the user query to an embedding vector, embed each fetched insight text, calculate cosine similarity using numpy, combine it with the on-chain reputation score (weighted 70% relevance + 30% reputation), and return only the top 3 matches as a clean JSON-like string with price, reputation, CID, and insight preview.
8. Add in-memory caching for instant repeated searches.
Wrap the entire tool function with @lru_cache(maxsize=32) (or use a simple dict cache with TTL) so the same query returns cached results in <100ms on subsequent calls during a single agent session.
9. Implement rate-limit and retry handling.
Decorate the tool with @retry(stop=stop_after_attempt(3), wait=wait_fixed(2)) and add a simple sleep check if Gemini returns 429; log the rate-limit event so the agent stays stable even under heavy TestNet usage.
10. Export the tool and test it independently.
At the bottom of semantic_search.py add if __name__ == "__main__": asyncio.run(semantic_search("latest NIFTY breakout pattern")) and run python -m backend.tools.semantic_search — it should query live TestNet contracts, fetch real IPFS insights, return the top 3 ranked results with scores, proving the tool is ready to be imported into the LangChain agent in the next point.